fastapi>=0.110
uvicorn[standard]>=0.29
joblib>=1.3
numpy>=1.26
pandas>=2.2
# 1.7.2로 정확히 고정 — reuse_model.joblib이 이 버전으로 학습됨. 다른 버전을 쓰면
# GradientBoosting 계열 모델(rul_model_B, fire_risk_model)의 pickle 로딩이 깨질 수 있다.
# charging-demand/README.md "알려진 이슈" 참고.
scikit-learn==1.7.2
openpyxl>=3.1
python-multipart>=0.0.9
anthropic>=0.40
reportlab>=4.0
requests>=2.31
