fastapi>=0.110
uvicorn[standard]>=0.29
joblib>=1.3
numpy>=1.26
# 1.7.2로 정확히 고정: 학습 당시 버전과 다르면 GradientBoostingRegressor의 내부 _loss
# 모듈 pickle 호환이 깨져서 joblib.load 자체가 실패한다(README "알려진 이슈" 참고).
scikit-learn==1.7.2
